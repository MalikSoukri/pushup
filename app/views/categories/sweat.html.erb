<div class="backcolor">
  <div class="banner-sweat-category">
    <div class="container">
      <div class="text-center">
        <h1><%= @sweat.name.upcase %></h1>
        <h2></h2>
        <%= link_to root_path do  %>
          <button type="submit" class="btn btn-flat buttoncategory col text-center">MY PLANS </button>
        <% end %>
      </div>
    </div>
  </div>
  <div class="carousel-container">
    <div class="carousel">
    <% @trainings.each_with_index do |training, index| %>
      <div class="carousel-item <%= 'active' if index == 0 %>" style="background-image: url('<%= cl_image_path training.photo_url %>')">
        <h1><%= training.name.upcase  %></h1>
      </p>
      </div>
      <% end %>
    </div>
  </div>
 
</div>

