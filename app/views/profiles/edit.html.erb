<div class="backcolor">
  <div class="container plan">
    
    
<%= simple_form_for [@profile] do |f| %>
      <div class="form container">
        <div class="form-inputs">
          <%= f.input :name, :label => "What's your profile name?" %>
          <%= f.input :age, :label => "What's the your age?" %>
          <%= f.input :description, :label=> "Tell us something about yourself" , as: :text%>
          <%# <%= f.input :rating, collection: 1..5, as: :radio_buttons %>
        <div class="justify-content-between pb-3">
          <%= f.input :photo, input_html: { class: 'd-none', id: 'photo-input'},
            wrapper_html: { class: 'mb-0 d-flex align-items-center'},
            label_html: { class: 'upload-photo mb-0'},
            label: '📷 Profile picture',
            as: :file %>
          <%= image_tag "", width: 200, class: "d-none mt-3 ", id: "photo-preview" %>
        </div>
          <div class="my-3" id="results">
            <%="Note: After clicking 'create', your profile will be updated" %>
          </div>
        </div>
      </div>
      <%= f.submit "Update",  class:"btn btn-flat mt-5 col text-center" %>
    <% end %>


  </div>
</div>  